import A;

class MethodChaining extends A {

    public static void main(String[] args) {

    }

    public int foo() {
        MethodChaining a;
        a = new MethodChaining();

        // Assume bar() is in A and bar2() is in bar(), same with another()
        a.bar().bar2().another();

        return 0;
    }
}
