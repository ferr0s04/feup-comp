import A;

class MethodChaining extends A {

    public static void main(String[] args) {

    }

    public int foo() {
        MethodChaining a;
        a = new MethodChaining();

        // Assume bar() is in A and bar2() is in bar()
        a.bar().bar2();

        return 0;
    }
}
